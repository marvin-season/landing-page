import { AgentConstant } from "@/lib/constant/agent";

/** Default agent ID for mastra operations (memory, chat, thread) */
export const AGENT_ID = AgentConstant.GENERAL_AGENT;

/** Default resource ID for memory/thread scoping (e.g. user ID) */
export const RESOURCE_ID = "user-1";

export { AgentConstant };
