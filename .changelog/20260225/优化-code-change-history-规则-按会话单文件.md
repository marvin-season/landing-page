- **文件**: `.cursor/rules/code-change-history.mdc`
- **修改内容**: 将单条变更对应单文件的规则改为「每个会话对应一个文件」；文件名由 `[总结标题].md` 改为 `[会话需求主题].md`；同一会话内多次修改（如先改主题色再加用户模块）合并到同一文件并追加记录；补充「同一会话」定义及首次/后续修改的区分说明。
- **原因/上下文**: 用户要求优化规则，减少同一会话内多需求产生的多文件，改为按会话聚合、单文件内保留多条需求记录。
