---
description: 代码修改后记录变更历史，修改前（跨会话）先读取 .changelog 以延续上下文
alwaysApply: true
---

# 代码变更历史规则

项目使用 `.changelog/` 目录记录代码变更，便于跨会话时 AI 或开发者延续上下文、减少重复和冲突。

## 目录与文件规则

- **日期子文件夹**：以当天日期为子文件夹名，格式为 `YYYYMMDD`，例如 `20260225` 表示 2026-02-25。
- **单文件规则**：当日所有变更**固定使用同一文件名** `index.md`，即 `.changelog/YYYYMMDD/index.md`。不按会话或主题拆分多个文件。
- **文件内容结构**：
  - **一级标题（`#`）**：根据当前文件内已记录的全部变更**动态总结**当日变化，用简短中文或英文概括（例如「规则与 changelog 目录调整」「agui 流式页配色与错误态优化」）。若为新建文件，可先写本次变更的概括，后续追加变更后可视情况更新该一级标题以覆盖全天内容。
  - **每次修改**：在文件中**追加一条二级标题（`##`）**，标题为本次变更的简短概括和时间戳（如「重命名 .history 为 .changelog (10:00:00)」「优化 rxjs 页 UI 配色 (14:30:00)」）；该 `##` 下按下面格式写本条记录。

目录结构示例：

```
.changelog/
  20260225/
    index.md
  20260224/
    index.md
```

## 一、每次修改代码之后

完成任意代码修改（包括 AI 生成或用户手动编辑）后，**必须**在 `.changelog/YYYYMMDD/index.md` 中记录本次变更：

1. 若当日尚无 `index.md`：新建该文件，先写一级标题（对本次变更的概括），再写一条二级标题及本条记录内容。
2. 若当日已有 `index.md`：在文件末尾**追加**一条二级标题（`## 本次变更概括`）及本条记录内容；可选地**更新文件顶部的一级标题**，使其成为对当前文件内所有变更的动态总结。

### 单条记录内容格式（每条变更一条 `##`，其下为以下内容）

每条变更在对应二级标题下建议包含：

- **文件**: 涉及的文件路径（多个可列多条）
- **修改内容**: 具体改了什么（新增/删除/重构了哪些部分）
- **原因/上下文**: 可选，为何这样改或关联的需求

`index.md` 内容示例：

```markdown
# 规则与 agui 页优化（当日变更摘要）

## 重命名 .history 为 .changelog
- **文件**: `.cursor/rules/code-change-history.mdc`，`.changelog/README.md`
- **修改内容**: 目录 `.history` → `.changelog`，规则与 README 中引用全部替换。
- **原因/上下文**: 用户计划将变更历史目录改为 .changelog。

## 优化 rxjs 页 UI 配色
- **文件**: `src/app/agui/rxjs/page.tsx`
- **修改内容**: 统一使用 primary/muted 主题变量，工具块由 amber 改为 primary，调整边框与背景透明度。
- **原因/上下文**: 用户要求优化页面配色。
```

## 二、下次修改代码之前（非同一会话时）

当满足以下情况时，视为**不在同一会话上下文**：

- 用户开启了新的 Cursor 对话（新 Composer/Agent 会话）
- 用户明确说「继续之前的修改」或「根据之前的改动」
- 当前对话中没有任何关于「刚才改了什么」的上下文

在此类情况下，**在动手改代码之前**应：

1. **先读取** `.changelog/` 下的近期变更记录：优先读当天日期子文件夹中的 `index.md`（如 `.changelog/20260225/index.md`），再按日期倒序读前几天的 `YYYYMMDD/index.md`。
2. 根据这些记录理解：最近动了哪些文件、做了哪些改动、为何这样改。
3. 再基于该上下文进行本次修改，并在本次修改完成后按「一」在当日日期文件夹下新增或追加到 `index.md` 记录。

这样可提升后续写代码的连贯性、减少重复劳动和与已有改动的冲突。

## 三、维护建议

- 按日期分子文件夹，便于按天查阅和清理；无需删除旧记录，可按需归档。
- 若希望将变更历史纳入版本控制，不要将 `.changelog/` 加入 `.gitignore`；若仅作本地参考，可加入 `.gitignore`。
