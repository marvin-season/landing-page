---
description: 代码修改后记录变更历史，修改前（跨会话）先读取 .history 以延续上下文
alwaysApply: true
---

# 代码变更历史规则

项目使用 `.history/` 目录记录代码变更，便于跨会话时 AI 或开发者延续上下文、减少重复和冲突。

## 目录与文件规则

- **日期子文件夹**：以当天日期为子文件夹名，格式为 `YYYYMMDD`，例如 `20260225` 表示 2026-02-25。
- **单条变更文件**：每次变更对应一个文件，文件名为 `[总结标题].md`。总结标题用简短中文或英文描述本次修改，文件名避免特殊符号，可用短横线或下划线，例如：`新增-chat-stream-状态-hook.md`、`修复登录逻辑.md`。

目录结构示例：

```
.history/
  20260225/
    新增-chat-stream-状态-hook.md
    修复-agui-页面报错.md
  20260224/
    重构-stream-状态管理.md
```

## 一、每次修改代码之后

完成任意代码修改（包括 AI 生成或用户手动编辑）后，**必须**在 `.history/YYYYMMDD/` 下新增一个以本次修改总结为标题的 Markdown 文件（`[总结标题].md`），并在其中记录本次变更摘要。

### 单条记录内容格式

文件内容建议包含：

- **文件**: 涉及的文件路径（多个可列多条）
- **修改内容**: 具体改了什么（新增/删除/重构了哪些部分）
- **原因/上下文**: 可选，为何这样改或关联的需求

若同一日内多次修改，每条变更单独一个 `[总结标题].md`，标题需区分（可加序号或更具体描述，避免重名）。

## 二、下次修改代码之前（非同一会话时）

当满足以下情况时，视为**不在同一会话上下文**：

- 用户开启了新的 Cursor 对话（新 Composer/Agent 会话）
- 用户明确说「继续之前的修改」或「根据之前的改动」
- 当前对话中没有任何关于「刚才改了什么」的上下文

在此类情况下，**在动手改代码之前**应：

1. **先读取** `.history/` 下的近期变更记录：优先读当天日期子文件夹（如 `.history/20260225/`），再按日期倒序读前几天的子文件夹中的 `*.md` 文件。
2. 根据这些记录理解：最近动了哪些文件、做了哪些改动、为何这样改。
3. 再基于该上下文进行本次修改，并在本次修改完成后按「一」在当日日期文件夹下新增 `[总结标题].md` 记录。

这样可提升后续写代码的连贯性、减少重复劳动和与已有改动的冲突。

## 三、维护建议

- 按日期分子文件夹，便于按天查阅和清理；无需删除旧记录，可按需归档。
- 若希望将变更历史纳入版本控制，不要将 `.history/` 加入 `.gitignore`；若仅作本地参考，可加入 `.gitignore`。
